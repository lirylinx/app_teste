

void main() {
  var halogens = const {'fluorine', 'chlorine', 'bromine', 'iodine', 'astatine'};
  var elements = <String> {};
  // elements.add('fluorine');
  // elements.addAll(halogens);

  // assert ( elements.length == 5);
  // print(elements.length);.

  // halogens.add('teste'); // BUG: Unsupported operation: Cannot change unmodifiable set
}



